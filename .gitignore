__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.log
.vscode/
.idea/

# Build output — scripts/build.ps1 stages the per-OS PyInstaller
# binaries and the shell-wrapper pair (copied from release/wrappers/)
# into bin/. Everything in there is reproducible from source; the
# release tarball is what ships to end users.
/bin/
*.pyinstaller.tmp/
