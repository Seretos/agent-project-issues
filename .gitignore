__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.log
.vscode/
.idea/

# Build output -- scripts/build.ps1 emits the PyInstaller binary
# directly to bin/project-issues (Linux) or bin/project-issues.exe
# (Windows). The release pipeline produces the same layout from the
# matrix artifacts. Everything in there is reproducible from source;
# the release tarball is what ships to end users.
/bin/
*.pyinstaller.tmp/
.process-ticket-result.json
